export default function AuthFooter() {
  return (
    <footer className="mt-6 pt-4 border-t border-gray-200 dark:border-gray-700 text-center text-xs text-gray-400 dark:text-gray-500 transition-colors duration-300">
      Demo credentials — no data is stored
    </footer>
  );
}
